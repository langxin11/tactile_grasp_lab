tactile_grasp_controller:
  ros__parameters:
    # 真机覆盖项：
    # 在基础配置 tactile_grasp_controller.yaml 之上叠加这些参数。
    dry_run: false

    # 真机抓取把控制频率提到 40 Hz，减轻 +1 步进带来的顿挫感，
    # 同时仍避免把 Robotiq 当作高频力控执行器。
    control_rate_hz: 40.0
